import {test} from 'node:test';import assert from 'node:assert/strict';import {verifyBackup} from '../scripts/verify-backup.mjs';import {legacyAccount} from './helpers.mjs';
const fixture=()=>({complete:true,readOnly:true,users:[{login:'anna',...legacyAccount('test-hash')}],kvEntries:[{name:'anna',value:JSON.stringify(legacyAccount('test-hash'))},{name:'uid:test',value:'anna'}],manifest:{accounts:1,addresses:1,records:1,kvKeys:2}});
test('backup verifier reconciles counts without revealing account contents',()=>{assert.deepEqual(verifyBackup(fixture(),{forMigration:true}),{accounts:1,addresses:1,records:1,kvKeys:2,chargedCents:15190,readOnly:true});});
test('backup verifier rejects partial, live, duplicate and mapping-less migration exports',()=>{for(const mutate of [b=>{b.complete=false;},b=>{b.readOnly=false;},b=>{b.manifest.records=2;},b=>{delete b.kvEntries;},b=>{b.users.push(b.users[0]);},b=>{b.users[0].addresses[0].records.push(b.users[0].addresses[0].records[0]);}]){const backup=fixture();mutate(backup);assert.throws(()=>verifyBackup(backup,{forMigration:true}));}});
